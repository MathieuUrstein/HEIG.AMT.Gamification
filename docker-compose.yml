version: '2'
services:
  mysql:
    build: ./images/mysql
    environment:
     - MYSQL_ROOT_PASSWORD=root
  open-jdk:
    build: ./images/open-jdk
    ports:
     - "9090:8080"
    links:
     - mysql:db
