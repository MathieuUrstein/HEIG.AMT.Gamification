version: '2'
services:
  mysql:
    build: ./images/mysql
    environment:
     - MYSQL_ROOT_PASSWORD=root
    ports:
     - "3306:3306"
  maven:
    build: ./images/maven
    links:
     - mysql:db
    ports:
     - "8080:8080"
