#!/bin/sh

set -u

. "$(dirname "$0")/env.sh"

echo "######################################################################"
echo "##                          TEST RESULTS                            ##"
echo "######################################################################"

cat "${REPORT_FILE}"

echo "######################################################################"
echo "##                        END TEST RESULTS                          ##"
echo "######################################################################"


RETURN=$(cat ${RESULT_FILE})

rm "${REPORT_FILE}"
rm "${RESULT_FILE}"

exit "${RETURN}"
